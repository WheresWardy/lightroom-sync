# ── Required ──────────────────────────────────────────────────────────────────

# Absolute path to the Lightroom Classic .lrcat catalog file.
# When running via docker-compose, set this to the path *inside* the container
# (the directory is mounted at /catalog — see LIGHTROOM_CATALOG_DIR below).
LIGHTROOM_CATALOG=/catalog/My Lightroom Catalog.lrcat

# Host directory that contains the .lrcat file.
# docker-compose mounts this as /catalog (read-only) inside the sync container.
LIGHTROOM_CATALOG_DIR=/path/to/lightroom/catalog/directory

# Base URL of your Immich instance (no trailing slash)
IMMICH_API_URL=http://localhost:2283/api

# Immich API key (create one in Immich → Account Settings → API Keys)
IMMICH_API_KEY=your_immich_api_key_here

# ── Optional ──────────────────────────────────────────────────────────────────

# Redis connection URL for caching Lightroom → Immich asset ID mappings
# Omit or leave blank to run without a cache (slower on large catalogs)
# When running via docker-compose, use the service name as the hostname:
REDIS_URL=redis://localhost:6379/0

# How long to keep cached asset mappings in Redis (seconds)
# 0 = store indefinitely   default: 604800 (7 days)
REDIS_CACHE_TTL=604800

# Maximum number of asset IDs sent per add-to-album API call
# default: 500
BATCH_SIZE=500

# Set to 1 to preview what would change without writing anything to Immich
# default: 0
DRY_RUN=0

# Logging verbosity: DEBUG, INFO, WARNING, ERROR
# --verbose / -v on the CLI will always override this to DEBUG
# default: INFO
LOG_LEVEL=INFO

# ── Sentry (optional) ─────────────────────────────────────────────────────────

# Sentry DSN for error reporting.
# If set, run.sh will use sentry-cli to report any non-zero exit code as an
# error event. Leave blank (or unset) to disable Sentry reporting.
# Install sentry-cli: https://docs.sentry.io/cli/installation/
SENTRY_DSN=
